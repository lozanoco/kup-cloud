
server:
  port: ${PORT:8002}

spring:
  application:
    name: kupproject-zuul

eureka:
  client:
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8001/eureka/}